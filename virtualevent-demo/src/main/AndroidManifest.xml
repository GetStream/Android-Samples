<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.getstream.chat.virtualevent">

    <application
        android:name=".VirtualEventApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VirtualEvent">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="io.getstream.chat.virtualevent.feature.event.EventDetailsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name="io.getstream.chat.virtualevent.feature.dm.DmActivity" />
        <activity android:name="io.getstream.chat.virtualevent.feature.dm.SelectUserActivity" />
    </application>

</manifest>