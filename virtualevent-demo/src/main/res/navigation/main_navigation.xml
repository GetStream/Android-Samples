<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="io.getstream.chat.virtualevent.MainFragment"
        android:label="MainFragment"
        tools:layout="@layout/fragment_main">

        <action
            android:id="@+id/action_to_selectUserFragment"
            app:destination="@id/selectUserFragment" />

        <action
            android:id="@+id/action_to_messageListFragment"
            app:destination="@id/messageListFragment">
            <argument
                android:name="cid"
                app:argType="string"
                app:nullable="false" />
        </action>

    </fragment>

    <fragment
        android:id="@+id/selectUserFragment"
        android:name="io.getstream.chat.virtualevent.feature.dm.SelectUserFragment"
        android:label="SelectUserFragment"
        tools:layout="@layout/fragment_select_user">

        <action
            android:id="@+id/action_to_messageListFragment"
            app:destination="@id/messageListFragment">
            <argument
                android:name="cid"
                app:argType="string"
                app:nullable="false" />
        </action>

    </fragment>

    <fragment
        android:id="@+id/messageListFragment"
        android:name="io.getstream.chat.virtualevent.feature.dm.MessageListFragment"
        android:label="MessageListFragment"
        tools:layout="@layout/fragment_message_list" />

</navigation>